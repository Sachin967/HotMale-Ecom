<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mobile Number Form</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to bottom, #E2E7F1, #C9D3E2);
      font-family: 'Poppins', sans-serif;
    }

    .mobile-number-form {
      width: 300px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 15px;
      margin-top: 150px;
      box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
    }

    h1 {
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 20px;
    }

    label {
      margin-bottom: 10px;
      font-size: 16px;
      font-weight: normal;
    }

    input {
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 10px;
      font-size: 16px;
      transition: border-color 0.3s ease-in-out;
    }

    input:focus {
      border-color: #4E9CAF;
    }

    button {
      background: linear-gradient(to bottom, #4E9CAF, #357D96);
      color: white;
      border: none;
      border-radius: 5px;
      padding: 10px;
      margin-top: 15px;
      cursor: pointer;
      transition: background-color 0.3s ease-in-out;
      margin-left: 90px;
    }

    button:hover {
      background-color: #0ea5d7;
    }

    .icon {
      margin-right: 10px;
    }
  </style>
</head>
<body>
  <div class="mobile-number-form">
    <h1>Enter Your Mobile Number</h1>
    <form action="/forgetpassword" method="post" id="mobileForm">
      <label for="mobile">
        <i class="fa fa-phone icon"></i> Mobile Number:
      </label>
      <input type="text" id="mobile" name="mobile" required>
      <button type="submit">Submit</button>
    </form>
  </div>
</body>
</html>

